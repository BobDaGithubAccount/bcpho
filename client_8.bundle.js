/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./src/client_8.ts":
/*!*************************!*\
  !*** ./src/client_8.ts ***!
  \*************************/
/***/ (() => {

eval("\n(function () {\n    const canvas = document.getElementById('challenge8_canvas');\n    const ctx = canvas.getContext('2d');\n    ctx.translate(canvas.width / 2, canvas.height / 2);\n    const gravityInput = document.getElementById('challenge8_gravity');\n    const heightInput = document.getElementById('challenge8_height');\n    const speedInput = document.getElementById('challenge8_speed');\n    const telemetry = document.getElementById('challenge8_telemetry');\n    const corInput = document.getElementById('challenge8_cor');\n    const bounceInput = document.getElementById('challenge8_bounces');\n    const angleInput = document.getElementById('challenge8_angle');\n    const modeInput = document.getElementById('challenge8_mode');\n    let scale = 1;\n    let offsetX = 0;\n    let offsetY = 0;\n    let isDragging = false;\n    let startX, startY;\n    let telemetryArray = [];\n    let sfx = 1;\n    let sfy = 1;\n    const curve = { points: [] };\n    let animationFrameId = null;\n    function drawAxes() {\n        ctx.clearRect(-canvas.width / 2, -canvas.height / 2, canvas.width, canvas.height);\n        ctx.save();\n        ctx.translate(offsetX, offsetY);\n        ctx.scale(scale, scale);\n        ctx.beginPath();\n        ctx.moveTo(-canvas.width, 0);\n        ctx.lineTo(canvas.width, 0);\n        ctx.strokeStyle = 'black';\n        ctx.stroke();\n        ctx.beginPath();\n        ctx.moveTo(0, -canvas.height);\n        ctx.lineTo(0, canvas.height);\n        ctx.strokeStyle = 'black';\n        ctx.stroke();\n        ctx.restore();\n    }\n    function calculateTrajectory() {\n        const g = parseFloat(gravityInput.value);\n        const h = parseFloat(heightInput.value);\n        const u = parseFloat(speedInput.value);\n        const restitution = parseFloat(corInput.value);\n        const maxBounces = parseInt(bounceInput.value);\n        const angle = parseFloat(angleInput.value) * (Math.PI / 180);\n        const timeStep = 0.01;\n        const points = [];\n        let vx = u * Math.cos(angle);\n        let vy = u * Math.sin(angle);\n        let x = 0;\n        let y = h;\n        let bounceCount = 0;\n        while (bounceCount <= maxBounces) {\n            x += vx * timeStep * sfx;\n            y += vy * timeStep * sfy;\n            vy -= g * timeStep;\n            if (y <= 0) {\n                y = 0;\n                vy = -vy * restitution;\n                bounceCount++;\n            }\n            points.push({ x, y });\n            if (y < -canvas.height / 2) {\n                break;\n            }\n        }\n        return points;\n    }\n    function drawCurve() {\n        if (!ctx)\n            return;\n        ctx.save();\n        ctx.translate(offsetX, offsetY);\n        ctx.scale(scale, scale);\n        ctx.beginPath();\n        let arcLength = 0;\n        for (let i = 0; i < curve.points.length; i++) {\n            const point = curve.points[i];\n            if (i === 0) {\n                ctx.moveTo(point.x, -point.y);\n            }\n            else {\n                const prevPoint = curve.points[i - 1];\n                const dx = point.x - prevPoint.x;\n                const dy = point.y - prevPoint.y;\n                arcLength += Math.sqrt(dx * dx + dy * dy);\n                ctx.lineTo(point.x, -point.y);\n            }\n        }\n        ctx.strokeStyle = 'blue';\n        ctx.stroke();\n        ctx.restore();\n        telemetryArray.push(`<p1>Blue Arc Length: ${Math.round(arcLength * 1000) / 1000}<p1>`);\n    }\n    function draw() {\n        curve.points = calculateTrajectory();\n        drawAxes();\n        drawCurve();\n        telemetry.innerHTML = telemetryArray.join('<br>');\n        telemetryArray = [];\n    }\n    function animate() {\n        if (animationFrameId !== null) {\n            cancelAnimationFrame(animationFrameId);\n        }\n        const points = calculateTrajectory();\n        let index = 0;\n        function step() {\n            if (index < points.length) {\n                ctx.clearRect(-canvas.width / 2, -canvas.height / 2, canvas.width, canvas.height);\n                drawAxes();\n                ctx.save();\n                ctx.translate(offsetX, offsetY);\n                ctx.scale(scale, scale);\n                ctx.beginPath();\n                for (let i = 0; i <= index; i++) {\n                    const point = points[i];\n                    if (i === 0) {\n                        ctx.moveTo(point.x, -point.y);\n                    }\n                    else {\n                        ctx.lineTo(point.x, -point.y);\n                    }\n                }\n                ctx.strokeStyle = 'blue';\n                ctx.stroke();\n                ctx.restore();\n                index++;\n                animationFrameId = requestAnimationFrame(step);\n            }\n            else {\n                telemetry.innerHTML = telemetryArray.join('<br>');\n                telemetryArray = [];\n            }\n        }\n        step();\n    }\n    function handleMouseDown(e) {\n        isDragging = true;\n        startX = e.clientX - offsetX;\n        startY = e.clientY - offsetY;\n    }\n    function handleMouseMove(e) {\n        if (isDragging) {\n            offsetX = e.clientX - startX;\n            offsetY = e.clientY - startY;\n            draw();\n        }\n        const rect = canvas.getBoundingClientRect();\n        const x = (e.clientX - rect.left - canvas.width / 2 - offsetX) / scale;\n        const y = -(e.clientY - rect.top - canvas.height / 2 - offsetY) / scale;\n        draw();\n        ctx.save();\n        ctx.translate(offsetX, offsetY);\n        ctx.scale(scale, scale);\n        ctx.fillStyle = 'black';\n        const points = curve.points;\n        if (points.length > 0) {\n            const closestPoint = points.reduce((prev, curr) => {\n                const prevDistance = Math.abs(prev.x - x);\n                const currDistance = Math.abs(curr.x - x);\n                return currDistance < prevDistance ? curr : prev;\n            });\n            ctx.beginPath();\n            ctx.arc(closestPoint.x, -closestPoint.y, 2, 0, 2 * Math.PI);\n            ctx.fillStyle = 'blue';\n            ctx.fill();\n            telemetryArray.push(`<p1>Blue: (${Math.round(closestPoint.x * 1000) / 1000}, ${Math.round(closestPoint.y * 1000) / 1000})<p1>`);\n        }\n        ctx.restore();\n    }\n    function handleMouseUp() {\n        isDragging = false;\n    }\n    function handleWheel(e) {\n        e.preventDefault();\n        const zoom = e.deltaY < 0 ? 1.1 : 0.9;\n        scale *= zoom;\n        draw();\n    }\n    function handleModeChange() {\n        if (modeInput.value === 'animation') {\n            animate();\n        }\n        else {\n            draw();\n        }\n    }\n    gravityInput.addEventListener('input', handleModeChange);\n    heightInput.addEventListener('input', handleModeChange);\n    speedInput.addEventListener('input', handleModeChange);\n    corInput.addEventListener('input', handleModeChange);\n    bounceInput.addEventListener('input', handleModeChange);\n    modeInput.addEventListener('input', handleModeChange);\n    canvas.addEventListener('mousedown', handleMouseDown);\n    canvas.addEventListener('mousemove', handleMouseMove);\n    canvas.addEventListener('mouseup', handleMouseUp);\n    canvas.addEventListener('wheel', handleWheel);\n    handleModeChange();\n})();\n\n\n//# sourceURL=webpack://bcpho/./src/client_8.ts?");

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module can't be inlined because the eval devtool is used.
/******/ 	var __webpack_exports__ = {};
/******/ 	__webpack_modules__["./src/client_8.ts"]();
/******/ 	
/******/ })()
;